# tbs 
# Зайдите в контейнер и запустите Python с профилированием импортов
docker exec -it test-app-1 bash

# Вариант 1: Профилирование импортов
python -X importtime -c "import app.main" 2> /tmp/import.log

# Потом посмотрите самые медленные импорты
grep "import time:" /tmp/import.log | sort -k2 -n -r | head -20

# Вариант 2: Трассировка sys.path
python -c "import sys; print('\n'.join(sys.path))"