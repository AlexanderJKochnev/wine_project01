# Доступ к сервису

1. Перейдите по адресу:  
   🔗 `https://83.167.126.4:8443`

2. При первом входе — ошибка безопасности (самоподписанный сертификат).

3. Чтобы убрать предупреждение:
   - Скачайте сертификат: [скачать nginx-selfsigned.crt](https://83.167.126.4:8443/cert.crt) *(если раздашь)*
   - Или запустите скрипт:
     - Windows: `install-cert-windows.ps1`
     - macOS: `install-cert-macos.sh`
     - Linux: `sudo cp cert.crt /usr/local/share/ca-certificates/ && sudo update-ca-certificates`

4. После — перезагрузите страницу.