# clickhouse
## Резюме по схеме: postgresl items содержит текстовую строку со всеми словами, 
## а clickhouse индексирует по этой строке - это не работает если база активно изменяется потому что: 
## индекс clickhouse при изменениях перестраивается целиком и грузит процессор
## до 1 млн записей fts postgresql работает не хуже;
## ждем наполнения базы затем принимаем решение

## RAG датабазе:
###   векторная база данных на clickhouse: 
###   поиск в ней по похожести
###   результат передается в LLM на генерацию текста. 
###   если результат неуд (отсутвуют данные) - тогда поиск в интернете и пополнение RAG


1. text (Inverted): Разбивает строку на слова (токены). 
   1. Лучший для поиска «содержит слово А и слово Б». Умеет ранжировать (через функцию score()), если использовать специальные движки, но в обычном MergeTree он просто дает сверхбыстрый фильтр.
   2. vector_similarity: Это база для следующего этапа (RAG). Позволяет искать «по смыслу» (косинусное сходство). Работает с эмбеддингами от LLM.
   3. bloom_filter / tokenbf_v1: Старое поколение. Вероятностные индексы. Могут давать «ложноположительные» срабатывания. Хуже чем text.
   4. ngrambf_v1 / sparse_grams: Поиск по подстрокам (не по словам, а по частям слов). Нужно, если вы ищете внутри слов (например, *ино* внутри виноград).

##  проверка видимости 
docker compose exec -it app python3 -c "import socket; s = socket.socket(); s.settimeout(2); print('SUCCESS' if s.connect_ex(('clickhouse_search', 9000)) == 0 else 'FAILED')"
